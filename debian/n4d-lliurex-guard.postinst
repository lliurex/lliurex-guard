#!/bin/sh

set -e
case "$1" in
   configure)
   
	   n4d-modules enable-plugin /etc/n4d/conf.d/LliurexGuardManager.json || true
	   deb-systemd-invoke restart n4d || true
	
   ;;

   abort-upgrade|abort-remove|abort-deconfigure)
   ;;

   *)
       exit 1
   ;;
esac

#DEBHELPER#

exit 0
